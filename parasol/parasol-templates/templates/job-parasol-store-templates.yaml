apiVersion: batch/v1
kind: Job
metadata:
  name: parasol-templates
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  backoffLimit: 100
  template:
    spec:
      containers:
      - name: parasol-templates
        command:
          - /bin/bash
          - '-c'
          - |
            set -x
            ansible-playbook -i localhost /scripts/playbook.yaml
        image: quay.io/agnosticd/ee-multicloud:latest
        volumeMounts:
          - mountPath: /scripts
            name: parasol-templates-vol
      restartPolicy: Never
      volumes:
      - name: parasol-templates-vol
        configMap:
          name: parasol-store-templates